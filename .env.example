# --- Auth / DB / Storage (Supabase) ---
# Required on Vercel:
# SUPABASE_URL=https://YOUR-PROJECT.supabase.co
# SUPABASE_ANON_KEY=...
# SUPABASE_SERVICE_ROLE_KEY=...

# Optional table names
# SUPABASE_PROFILES_TABLE=wochenbericht_profiles
# SUPABASE_ENTRIES_TABLE=wochenbericht_entries

# Optional app fallback user id (must be UUID when using Supabase DB without auth)
# APP_DEFAULT_USER_ID=00000000-0000-0000-0000-000000000000

# --- Export file storage (Supabase Storage) ---
# Bucket for generated xlsx/pdf files
# SUPABASE_EXPORTS_BUCKET=wochenbericht-exports
# Set to 1 if bucket is public (otherwise signed URLs are generated)
# SUPABASE_EXPORTS_PUBLIC_BUCKET=0
# Signed URL lifetime in seconds (default 86400)
# SUPABASE_EXPORTS_SIGNED_URL_TTL_SECONDS=86400

# --- Worker export backend (recommended for Vercel) ---
# Full worker endpoint URL, e.g. https://your-worker.example.com/export-week
# EXPORT_WORKER_URL=
# Shared secret used by Vercel app and worker
# EXPORT_WORKER_TOKEN=

# --- Template source ---
# Local template path for development
TEMPLATE_XLSX_PATH=.\\examples\\empty\\AXIANS OFM Wochenbericht Februar 2026 KW 9.xlsx
# OR remote URL (e.g. Supabase Storage public/signed URL)
# TEMPLATE_XLSX_URL=

# --- Local development exporter (only used when EXPORT_WORKER_URL is unset) ---
# Optional: Set if `python` is not on PATH
# PYTHON_BIN=python
# Optional: LibreOffice for PDF export (local only)
# SOFFICE_PATH=C:\\Program Files\\LibreOffice\\program\\soffice.exe

# --- Deployment flags ---
# Disable auth completely (local only)
# AUTH_DISABLED=1
# Force local file DB/storage even when Supabase is configured (local only)
# DB_BACKEND=local
# STORAGE_BACKEND=local
# Disable PDF export (recommended on Vercel until worker has LibreOffice)
# DISABLE_PDF_EXPORT=1
# Client-side UI flags (match server flags for cleaner UI)
# NEXT_PUBLIC_DISABLE_PDF_EXPORT=1
# NEXT_PUBLIC_DISABLE_EXPORTS=0
